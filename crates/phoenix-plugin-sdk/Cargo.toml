[package]
name = "phoenix-plugin-sdk"
version = "0.2.0"
edition = "2021"
license = "MIT"
description = "SDK for building Phoenix WASM tool plugins"

[dependencies]
wit-bindgen = "0.41"
serde_json = "1"
phoenix-shared = { path = "../phoenix-shared" }
