[package]
name = "phx-shared"
version = "0.1.0"
edition = "2024"
license = "MIT"
description = "Shared types for phx core and plugin SDK"
repository = "https://github.com/duckedup/phx"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
